# SkinCheck
An end-to-end skin cancer detection system built using a CNN model in TensorFlow. The application classifies skin lesions as benign or malignant, highlights important regions using Grad-CAM, and generates context-aware diagnostic explanations using an LLM, integrated through a React and Flask web interface.
